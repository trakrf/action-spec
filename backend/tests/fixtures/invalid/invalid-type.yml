apiVersion: actionspec/v1
kind: WebApplication
metadata:
  name: test-type-error
spec:
  compute:
    tier: web
    size: demo
    scaling:
      min: "one"  # ← Invalid! Should be integer
      max: 3
  network:
    vpc: vpc-test
    subnets: []
    securityGroups: []
  security:
    waf:
      enabled: false
    encryption:
      atRest: true
      inTransit: true
  governance:
    maxMonthlySpend: 10
    autoShutdown:
      enabled: false
