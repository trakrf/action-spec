# Missing required field: apiVersion
kind: WebApplication
metadata:
  name: test-invalid
spec:
  security:
    waf:
      enabled: false
    encryption:
      atRest: true
      inTransit: true
  governance:
    maxMonthlySpend: 10
    autoShutdown:
      enabled: false
